/* ============================================================
   Forgeline — one place to connect your database.

   Paste your Supabase Project URL and anon (public) key below,
   then commit & push. That's it — the website form, the Leads
   dashboard, Orders and the Quote maker all read from here.

   Where to find these: Supabase → Project Settings → API.
   The anon key is safe to be public (Row Level Security limits it
   to inserting new leads only). See supabase/README.md.
   ============================================================ */

window.FG_SUPABASE_URL      = 'https://tcxhpvyhiujsrsjgvguu.supabase.co';
window.FG_SUPABASE_ANON_KEY = 'sb_publishable_hB01lUwseQdJK5jGm01PEA_cR0iPuuL';
